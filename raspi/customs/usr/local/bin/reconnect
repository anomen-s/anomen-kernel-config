#!/bin/sh

DATE=`date`

LOG=/var/tmp/temp.log
echo $DATE >> $LOG
/opt/vc/bin/vcgencmd measure_temp >> $LOG

LOG=/var/tmp/reconnect.log

echo $DATE >> $LOG
if ping -q -c 3 comtrend >/dev/null
then
 echo OK >> $LOG
else
 echo reconnecting >> $LOG
 ifdown wlan0
 sleep 5 
 ifup wlan0
fi

